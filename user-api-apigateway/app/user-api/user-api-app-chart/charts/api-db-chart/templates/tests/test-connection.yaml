apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app-db-chart.fullname" . }}-test-connection"
  labels:
    {{- include "app-db-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "app-db-chart.fullname" . }}:{{ .Values.service.appPort }}']
  restartPolicy: Never
